# ModHub 更新日志

本文件记录 ModHub 模组管理套件的版本演进历史，按版本由新到旧倒序排列。

---

## v1.0.1 (2026-09-26)

### 优化性更新

1. **模组市场连续安装体验**：从模组市场一键安装模组后选择【稍后重载】，界面不再自动跳离市场页签，玩家可以留在市场中连续浏览并安装多个模组，全部安装完成后手动重新载入即可生效（本地导入模组的「切换管理页并金色高亮」原有设计保持不变）。
2. **历史版本永久保留**：`release/` 归档目录与 GitHub Releases 中的历史版本安装包不再随新版发布而清理，任何旧版本均可追溯与回滚（`../MOD/` 游戏生效目录仍保持单一最新版，防止同名模组多版本并存引发加载冲突）。
3. **管理页模组简介智能补全与动态回填**：针对第三方模组作者未在 `boot.json` 声明中文简介或副标题的情况，全面优化管理页的简介呈现体验。
   - 扩充主流社区模组内置对照词库（涵盖 `ModHub`、`GuideToMe`、`DoLSims`、`Wraith'sReflection`、`NoBusHarassmentMod`、`SimpleFramework`、`Cattery`、`FishingLife` 等数十款模组）；
   - 引入大小写、空格、下划线及单双引号全容错归一化查找机制（`dolOptFindKnownAlias`）；
   - 引入模组市场数据动态回填机制：当本地模组无自带简介且未命中静态词库时，自动联动模组市场统一索引与缓存数据，提取其中文名称或功能简述回填展示；
   - 为 ModHub 自身 `boot.json` 补充中文昵称与描述。
4. **TweeReplacer 补丁冲突快速诊断增强**：在模组加载异常快速诊断中，新增针对 `TweeReplacer` 补丁匹配失败与冲突的精准诊断知识库（`twee-patch-mismatch`）。自动提取报错模组名称与目标段落名称（支持 `[段落] Widgets Clothing Caption` 徽章一键筛选），通俗解析模组间补丁冲突与游戏版本文本差异成因，针对界面入口改写冲突提供专属排查建议。
5. **补丁文件 Unix LF 换行归一化**：全面将 `src/twee/modloader/` 下的所有 `.twee` 补丁文件转换为标准 Unix LF 换行，新增 `.gitattributes` 锁定 LF，并在 `pack.py` 打包时自动确保 `.twee` 规范为 LF 写入，彻底消除因 Windows CRLF 导致后置第三方模组（如原版优化 v1.1.0.6）严格纯文本 LF 匹配断裂的隐患。
6. **顶部操作按钮布局顺序调优与视觉统合**：将【智能整理模组与美化顺序】按钮调整至【刷新列表】左侧，突出高频的智能整理操作；同时为【刷新列表】按钮补齐 `dol-opt-btn-primary` 高亮样式类，实现顶部操作栏全套按钮均具备统一的原生金色边框暗黑高质感。

### 漏洞修复

1. **原版优化图像包占用与依赖解耦修复**：修复与「原版优化」(DolOptimization) 一起安装时原版优化图像无法正常显示的问题（感谢社区贡献者 @anlin.studio 提交 PR 修复）。
   - 彻底移除 `src/boot.json` 中遗留的 `BeautySelectorAddon` 图像插件配置（`(请确保启用此图像包) 原版优化 DolOptimization`），杜绝无图包的 ModHub 抢占原版优化图像包注册槽位；
   - 移除 `dependenceInfo` 中对 `BeautySelectorAddon` 和 `maplebirch` 的非必要强依赖，仅保留核心 `TweeReplacer` 依赖，实现与原版优化的彻底解耦与零冲突并存。
2. **已安装模组未被模组市场正确识别修复**：修复 `GuideToMe`（对应市场【控制NPC嘴部】）、`DoLSims`（对应市场【模拟人生】）、`Wraith'sReflection`（对应市场【怨灵的倒影】）等模组在本地安装后，模组市场仍显示为未安装、按钮仍显示为【下载安装】的问题。
   - 引入通用的 DoL 生态前缀后缀剥离算法（`stripDoLPrefix`），智能消除仓库名中普遍存在的 `DOL-`、`Degrees-of-Lewdity-`、`scml-` 前缀及 `-mod`、`_mod` 后缀干扰，实现核心标识的自动精准比对；
   - 归一化算法（`normalizeKey`）增强对单双引号（`'`、`’`、`"`、`“`、`”`）与特殊标点的过滤，彻底消除带引号模组（如 `Wraith'sReflection`）的比对偏差；
   - 全面扩充社区知名模组全能别名字典（`DOL_OPT_KNOWN_MOD_MARKET_ALIASES`）与离线仓库映射（`DOL_OPT_OFFLINE_REPOSITORIES`），打通中英文双向映射通道；
   - 将模组匹配查找（`findMarketModByLocalName`）阈值与市场判定严格对齐（80分），确保已安装状态判定零漏报。
3. **已移除 GitHub 仓库与无 Release 模组防误报更新**：修复模组作者删除 GitHub 仓库或未在 GitHub 发布 Release 时，模组市场因 Wiki 历史文本版本误判而错误显示【发现新版】与【一键更新】的问题。
   - 引入通用的仓库有效性检测机制与失效仓库（Dead Repositories）管理体系，支持作者名模糊关联、仓库名解析与 HTTP 404 即时识别，全面覆盖同作者名下全系失效模组（包括 `Kanna-hanabi/WovenRealm`、`Kanna-hanabi/WovenRealmUI` 等关联模组）；
   - 视觉呈现全新引入符合 DoL 暗黑红调色体系的**红色【源已失效】徽章**（`.badge-dead-repo`），卡片清晰标示失效状态，已安装模组操作按钮安静置灰为【已安装】，未安装模组置灰为【暂无下载】；
   - 强化可更新判定（`checkModInstallStatus`），仅在远端具备真实可下载的 Release 产物时才允许触发更新提醒，杜绝纯 Wiki 静态参考版本引发的虚假更新；
   - 运行时遭遇 404 移除时自动友好提示并即时矫正本地状态与界面徽章，彻底杜绝点击更新必然报错且无法自愈的异常死循环。
4. **合法单字符仓库防误诊与缓存自愈清洗**：修复仓库名为单个短横线连字符（如【牧场与你相识的故事】`dawalizhang/-` 与【全新空间】`lingyu230514/-`）被误判为失效源并显示红色标签的问题。
   - 彻底移除对连字符仓库名的错误规则拦截，恢复合法单字符仓库的正常 Release 检索与下载安装；
   - 建立活跃仓库白名单保护，并在市场启动时自动对本地历史存储中的误诊条目执行自愈清洗。
5. **模组市场纯英文缩写偶合误判与 ReadMe 串联修复**：修复未安装 `D.O.L.I` 模组时，模组市场错误将其检测为已安装（且已装版本误读为 `v2.101.0`）、以及本地底层前置模组 `ModLoader DoL ImageLoaderHook` 的 ReadMe 文档页被错误附加上 `D.O.L.I` 市场资料与仓库地址的问题。
   - 根因排查：长词双向包含匹配规则中，纯 ASCII 短串（如 `doli` 长度仅 4）在缺乏单词边界防御时，偶然命中了长技术标识 `modloaderdolimageloaderhook` 中的 `...dol` + `i...` 跨词字母拼接；
   - 匹配防御加固：纯 ASCII（英文/数字）环境严格禁止短于 8 字符的子串跨词匹配，且覆盖率必须达到 80% 以上；中文语义环境下限制最短长度与覆盖率阈值；
   - 彻底杜绝纯英文字符串局部碎片拼合导致的假阳性跨模组串连与版本混淆。

---

## v1.0.0 (2026-09-25)

首个独立发布版本。MOD 管理器模块自「原版优化」(Dol-Optimization) v1.1.0.5 完整解耦，以 ModHub 独立模组形态发布，与原版优化模组彻底分离、互不影响。

### 新功能

1. **模组管理**：旁加载模组的启用、禁用、删除、拖拽排序与智能依赖拓扑排序（含 nickName 别名解析与循环依赖安全回退）。
2. **模组市场**：Wiki 模组列表获取、GitHub Release 检索、国内镜像加速与故障自动切换、本地安装状态智能比对、一键下载旁加载安装。
3. **ReadMe 浏览器**：离线与在线双源 Markdown 渲染阅读，模组内置图片解码与网络图片代理。
4. **加载日志**：启动错误全局捕获、结构化错误诊断、关键词过滤导航与诊断长图一键生成导出。
5. **美化包管理**：承接 BeautySelectorAddon 图像包排序管理与自动启用联动。

### 优化性更新

1. **ModLoader v2.101.1+ 兼容层**：`window.modLoaderGui` 移除后自动映射至 `modModLoadController` + `modUtils` 新 API，旧版调用零感知平滑降级。
2. **持久化安全**：模组列表写入前与存储真实记录合并（已安装未登记模组自动保留防假删除）、写入后回读校验杜绝假成功、`dropNames` 语义保证删除模组不被复活。
3. **孤儿包体自愈**：启动时自动修复「包体已安装、未登记进启用列表」的历史遗留模组。
4. **游戏原生暗黑模态框**：`dolOptConfirm` / `dolOptAlert` 全面替代浏览器原生弹窗，支持危险操作红色语义与键盘操作。
5. **移动端适配**：拖拽排序触控手柄、边缘 RAF 平滑自动滚动、窄屏防挤压布局与底部安全边距。

### 漏洞修复

1. 修复 DataTransfer 构造不受支持环境下批量导入崩溃的问题（自动回退文件数组直传）。
2. 修复市场安装在管理器保存配置期间写入被拒导致的「安装未完成」无提示问题（安装前等待管理器空闲）。
3. 修复安装失败原因不透传的问题（`_dolOptLastInstallError` 诊断信息上报）。

### 解耦变更说明

- 模组标识：`name` 由「原版优化」变更为 `ModHub`，TweeReplacer 补丁 tip 统一改用【ModHub】前缀。
- 文件边界：仅包含模组管理器相关文件（`modloader-optimization.js`、`dol-mod-market.js`、`modloader-optimization.css`、`twee/modloader/*`），不携带原版优化的任何游戏性修改、图像资源与 Twee 补丁。
- 依赖承接：`BeautySelectorAddon` 与 `maplebirch` 依赖声明由 ModHub 承接（原版优化侧已同步移除）。
- 品牌更新：诊断长图水印与模块头部注释统一更新为 ModHub 标识。
