# ModHub 更新日志

本文件记录 ModHub 模组管理套件的版本演进历史，按版本由新到旧倒序排列。

---

## v1.0.2 (2026-09-26)

### 新功能

1. **模组安装兼容性与互斥冲突检测引擎**：
   - 建立高扩展性模组冲突规则库（`KNOWN_MOD_CONFLICT_RULES`），原生内置主流框架互斥规则（如【秋枫白桦框架 / 枫叶框架】与【简易框架】互斥）。
   - 在模组安装确认页全面引入实时兼容性检测：
     - 精准检测即将引入的目标模组及待安装前置依赖是否与本地已有模组存在互斥冲突；
     - 智能识别本地冲突模组的实际状态（【本地冲突已启用·高风险】或【本地已安装·当前禁用】），并提供详细冲突原因解析与实操排查建议；
     - 全面支持**英文标识智能回退中文化**（例如纯英文技术标识 `maplebirch` 自动解析为友好的中文名称【秋枫白桦框架】）；
     - 精简建议文字呈现，彻底消除提示前缀与正文开头的“建议：建议...”重复字样。
   - **冲突卡片快捷禁用与依赖影响评估**：
     - 在安装确认页的冲突警告卡片中，为本地已启用的冲突框架直接提供【快捷禁用】操作按钮；
     - **全方位依赖反向引用影响评估**：点击禁用前自动深度排查本地所有已启用的模组，智能检索其 `dependenceInfo`、`addonPlugin` 及其他依赖声明；若存在下游依赖该框架的模组，以原生暗黑模态框详细罗列受影响模组清单并提示潜在报错风险，由玩家最终确认；
     - 确认禁用后无缝调用底层模组控制器持久化禁用，原弹窗内部即刻实时刷新冲突状态与按钮样式，免除玩家离开安装流程往返设置的繁琐步骤。
   - **动态联动排除**：
     - 若冲突源于某个前置依赖（如安装日落伊甸园默认勾选安装简易框架，而本地已有枫叶框架），玩家取消勾选该前置依赖后，冲突警示卡片即时响应消除，动态转为绿色安全提示（【已取消勾选冲突前置模组，本次安装冲突风险已排除】）；
     - 重新勾选时警告立即恢复，让玩家在安装前即可自主排除环境风险；
   - **安全双重兜底护栏与显眼高亮警示**：
     - 若玩家在存在高风险冲突（本地已启用冲突模组）的情况下执意点击确认安装，系统将弹出游戏原生暗黑风险确认模态框进行二次拦截；
     - **视觉高亮重磅升级**：二次确认模态框中间的冲突提示升级为原生暗黑红光高亮警示盒（`.dol-opt-modal-conflict-alert-box`），互斥模组金红分明加粗高亮，危害后果以加深虚线与显眼红字醒目呈现，大幅强化风险传达；
     - **按钮文案优化与 5 秒冷静倒计时禁用防护**：二次确认弹窗中的确认按钮调整为更加严谨中立的【继续安装】（倒计时显示为 `继续安装 (5s)`），倒计时期间禁用点击并拦截 Enter 快捷键，倒计时结束后自动解除禁用，强制引导玩家充分审视冲突隐患，防止不假思索误点继续；若选择取消则安全停止，若选择继续安装则尊重玩家决定继续执行。
2. **模组管理页启用冲突检测引擎**：
   - 在【模组管理】页面点击【启用】模组时（例如本地已启用秋枫白桦框架，此时手动尝试启用简易框架，或反之），底层引擎即时介入进行互斥检测；
   - 检出冲突时弹出原生暗黑模态框（【模组冲突风险确认】），以红框警示盒醒目标注即将启用的模组与当前已启用的冲突模组以及互斥原因；
   - 同样配置 5 秒冷静倒计时禁用（【继续启用 (5s)】），玩家取消则终止操作并保持禁用状态，玩家确认后才继续执行启用并保存配置。
3. **模组删除下游依赖影响全面排查与安全护栏**：
   - 在【模组管理】页面点击【删除】任何模组或框架时，新增全量依赖分析引擎（`window.dolOptFindDependentMods`），深度扫描本地所有模组的 `dependenceInfo`、`addonPlugin` 及其他依赖声明；
   - 若存在依赖该模组的下游模组，删除确认模态框自动切换为【确认删除模组（存在依赖警告）】，以加粗红字警示盒详细罗列全部受影响的下游模组名称、版本及当前状态，并启动 5 秒倒计时禁用机制防手滑误删；无下游依赖时则维持原有简洁确认体验。
4. **底层模态对话框架构升级支持多层弹窗堆叠（Modal Stack）**：
   - 彻底重构 `window.dolOptConfirm` 与 `window.dolOptAlert`，淘汰旧版写死单一 DOM 节点强制销毁旧弹窗的设计，建立自递增层级的活动模态弹窗栈（`dolOptModalStack`）。
   - **平滑嵌套与状态安全**：支持在父弹窗（如安装配置窗口）上方叠加子弹窗（如快捷禁用影响评估框），子弹窗关闭后父弹窗完整存留，按键与事件监听精准作用于顶层活动弹窗；彻底修复了在安装弹窗中点击快捷禁用后主弹窗丢失导致卡片长期挂起在“正在生成清单”状态的隐患。
5. **前置依赖可选勾选与动态联动安装**：
   - 将模组安装确认页中的前置依赖处理机制由“强制全部安装”优化为“可供灵活勾选”（默认全部勾选）。
   - 玩家可根据自身游戏环境与调试需求，自由勾选或取消勾选个别前置依赖，安装流程将严格按拓扑依赖顺序仅处理玩家选中的前置项，未勾选项安全跳过且不阻断目标模组的正常安装。
   - 弹窗底部操作按钮与各前置条目的状态标签实现实时数据联动：勾选时高亮显示待处理项并动态显示项数（如【一并安装（含 2 项依赖）】），取消勾选时平滑切换为灰色【跳过处理】提示并自适应更新按钮文案（如【开始安装】或【直接打开下载页面】）。

6. **安装与冲突排查交互细节全面优化**：
   - **按钮文案更符合直觉**：安装确认弹窗中的主操作按钮由“一并安装”优化为“一键安装”，直观传达一站式处理依赖的便捷性。
   - **标题数量标记减负**：去除前置依赖标题（`前置依赖`）与冲突卡片标题（`检测到已知模组冲突`、`冲突已排除 · 兼容性检查通过`）中不美观的 `(1)` 括号数字，视觉呈现更加清爽大方。
   - **冲突即时排除的绿色安全反馈**：
     - 在安装确认弹窗中点击【快捷禁用】排除冲突模组后，冲突警示卡片即刻变绿（转为 `.dol-opt-install-conflict-card.is-resolved` 风格），显示绿色【检查通过】徽章与安全说明；
     - 在模组管理界面的启用冲突弹窗中点击【快捷禁用】后，警示盒同样无缝切换为绿色通过样式，5 秒倒计时安全锁即刻提前解除，主按钮转为金色【确认启用】。
   - **核心框架状态变更强烈重启建议引擎**：
     - 当玩家安装、启用、快捷禁用或卸载底层核心框架（如秋枫白桦框架、简易框架等互斥框架）时，由于底层挂钩机制与 ModLoader 运行时状态强相关，系统自动弹出带有强烈建议重载确认框（【重新载入游戏（强烈建议）】）；
     - 醒目警示盒告知玩家核心框架底层机制与冲突检测功能强绑定，强烈建议立即重启游戏刷新系统状态，确保后续模组依赖分析与运行完全正常。
   - **移动端“手机端 (左侧)”关闭按钮适配与修复**：
     - 彻底修复游戏设置中开启“弹窗中关闭按钮的位置: 手机端 (左侧)”时，右上角关闭按钮消失且左侧按钮被 ModHub 误隐藏的重大缺陷；
     - 标签栏过滤引擎精准识别移动端关闭按钮（`<<closeButtonMobile>>`），赋予其专属紧凑醒目的红色按钮样式（`.dol-opt-mobile-close-tab`），并自适应消除标签栏右侧留白，确保手机端玩家单手便捷关闭模组管理器。

### 漏洞修复

1. **修复简易框架副标题/简介误识别为秋枫白桦框架（枫叶框架）的缺陷**：
   - 针对部分发行包将模组名称声明为复数带空格的 `Simple Frameworks` 导致本地别名查表落空、进而回退至停用词模糊检索并因共享词根误判为秋枫白桦的问题，彻底补全 `Simple Frameworks`、`SimpleFrameworks`、`Simple Framework` 在已知别名库（`DOL_OPT_KNOWN_MOD_ALIASES`）、市场映射表（`DOL_OPT_KNOWN_MOD_MARKET_ALIASES`）及 `mod-identities.json` 中的双向权威映射；
   - 在停用词表（`GENERIC_STOP_WORDS`）中追加复数字符串 `frameworks`，彻底消除跨语言语义包含误伤假阳性；
   - 确保简易框架安装后在模组列表中副标题百分之百准确呈现为【简易框架】。
2. **修复依赖简易框架的模组在禁用检测时被误归入秋枫白桦框架下游的缺陷**：
   - 根因分析：秋枫白桦框架发行包在 `boot.json` 中声明了 `"alias": ["Simple Frameworks"]`，导致在构建受影响依赖目标键时对手冲突组被意外混入，进而导致依赖简易框架的模组（如自定义染发 CustomHair）在禁用/删除秋枫白桦时被误诊为依赖秋枫白桦；
   - 架构修复：在依赖分析引擎（`findDependentModsForConflict` 及 `window.dolOptFindDependentMods`）中引入严格的**对手冲突组隔离机制（Opponent Conflict Group Isolation）**。从目标的有效键集中彻底过滤对手组别名，并在判定模组依赖声明时设立对手组防线，彻底消除互斥框架间的别名交叉污染与下游依赖误诊。
3. **修复一键安装多前置依赖时中途弹出重载提醒打断后续下载的缺陷**：
   - 根因分析：在市场执行多步骤安装计划（如一键安装包含核心框架与模组本体）时，底层在安装/启用完第一个前置框架模组后，便提前无条件触发了重载确认强提醒，导致主流程被意外打断，后续模组本体无法开始下载；
   - 架构修复：
     - 在多步安装执行引擎（`executeInstallPlan`）中引入全流程静默守护机制：前置依赖启用传递 `{ silentOfferReload: true }`，前置与目标模组下载安装统一传入 `{ askRestart: false, skipReloadOffer: true }`，并在底层 `dolOptHandleAddMod` 中加入严格的 `skipReloadOffer` 与 `keepCurrentTab` 拦截守护，确保所有依赖与本体全部下载并落盘完毕前绝不擅自弹窗，同时严格保持在模组市场页签；
     - 在整个批次安装落盘完毕后，在顶层统合检测本次处理的全部模组（目标本体 + 执行过的所有前置依赖）中是否包含核心框架；若包含则统一触发一次带有强烈重启建议的重载确认框，若不包含则弹出常规重载确认框，实现连续流畅、无缝打断的一站式下载安装体验。
4. **修复模组管理页启用简易框架被误识别为启用秋枫白桦框架（自身与自身冲突）的缺陷**：
   - 根因分析：秋枫白桦框架在 `boot.json` 中声明了 `"alias": ["Simple Frameworks"]`，当 ModLoader 运行时查询 `Simple Frameworks` 时别名映射返回了秋枫白桦（`maplebirch`）对象；旧版冲突组判定引擎同时匹配了双方组别，三元判断将待启用模组和已冲突模组双双认定为秋枫白桦，并在弹窗中错误显示为“即将启用：【秋枫白桦框架】”、“当前已启用冲突模组：【秋枫白桦框架】”；
   - 架构重构：
     - 确立**直接字面名称高精比对（Direct Literal Match）最高优先级**，彻底杜绝跨组别名串门；
     - 设立**对手冲突组专属特征排除防线（Opponent Rejection）**，凡名称包含对手特征直接互斥排除；
     - 彻底封死 `boot.alias` 用于当前模组身份判定的漏洞（严禁通过对手别名反认自身）；
     - 引入展示名称防污染守护（`getCleanDisplayName`），当模组确定归属于简易框架组时强制锁定展示为其标准中文名【简易框架】，彻底屏蔽 ModLoader 别名重定向导致的秋枫白桦副标题污染；
     - 同组内（如已启用 simpleframework 时再启用 Simple Frameworks）不产生虚假互斥冲突。
5. **模组管理页列表禁用按钮补齐下游依赖检查**：
   - 模组列表中的【禁用】操作与快捷禁用保持一致，点击禁用前自动调用 `dolOptFindDependentMods` 排查本地所有已启用的模组；若存在依赖该模组的下游活跃模组，弹出二次确认警示框列出受影响模组清单，防止玩家误禁用底层前置导致下游模组报错。
6. **精简模组启用与禁用 Toast 提示**：
   - 移除启用/禁用成功后 Toast 提示中的冗余文案 `（原排序保持不变）`，使提示更加简洁自然。
7. **修复模组市场中【ModHub模组管理中心】显示为“待分类”的缺陷**：
   - 在 `mod-identities.json` 与离线仓库表中补全 ModHub 权威身份与分类；
   - 在分类规则库与标签规则库中扩展对 `modhub`、`mod-hub`、`manager`、`qol` 等管理类与便利类标识的智能识别，确保 ModHub 及其相关管理工具百分之百准确归入【界面与便利】分类。

### 优化性更新

1. **安装确认弹窗前置依赖视觉呈现全面升级**：
   - 彻底解决旧版前置依赖以单行纯白文本展示、文字偏小且状态不明显的问题，引入结构化暗黑胶囊卡片布局。
   - **DoL 原生多色状态指示**：
     - **已满足/已就绪**：采用鲜明绿色（`green`），显示绿色圆点与绿色微光胶囊标签，清晰标示当前本地环境已满足要求且无需额外下载；
     - **未安装/待安装**：采用醒目金色（`gold`），高亮标示待安装模组与版本要求；
     - **需更新/待更新**：采用醒目金色（`gold`），详细展示本地已有版本与目标要求版本的比对信息；
     - **未启用/待启用**：采用优雅紫色（`purple`），提示模组已在本地但处于禁用状态；
     - **跳过安装/跳过处理**：取消勾选时自适应切换为低调灰阶（`grey`）与行半透明弱化。
   - **字号与排版呼吸感调优**：
     - 依赖标题字号放大至 `1.08em`（与下方安装文件概览标题完美对齐），正文字号适度提升至 `0.96em` ~ `1em`，模组名称加粗呈现；
     - 增加触控友好的弹性项高（最小高度 38px，整行可点击切换勾选），全面适配手机端与竖屏窄屏设备，防挤压防变形。
2. **冲突警示视觉设计（DoL 原生暗黑风格）**：
   - 采用低饱和深红灰渐变底色结合 `rgba(239, 68, 68, 0.45)` 微红边框，搭配醒目红色【兼容性警告】胶囊徽章与金色模组高亮标签；
   - 适配移动端与窄屏设备，整行内容自适应换行防挤压，且严格遵循 0 Emoji 规约。
3. **底层模态对话框交互扩展能力增强**：
   - 为游戏原生暗黑模态框 `window.dolOptConfirm` 新增 `customResult` 与 `onRender` 选项支持，允许外部在弹窗挂载后绑定事件响应与提取自定义表单数据，保持百分之百向下兼容。
4. **模组列表禁用操作全面引入下游依赖排查引擎**：
   - 彻底解决在【模组管理】列表中点击【禁用】模组时跳过下游依赖检查、直接落盘并弹出重启强提示的缺陷；
   - 玩家在列表点击【禁用】时，引擎自动调用 `window.dolOptFindDependentMods` 进行深度排查；若检测到存在依赖于该模组/框架的下游模组，自动弹出原生暗黑模态框详细罗列所有受影响模组名称及版本，由玩家最终确认；玩家选择【暂不禁用】可安全取消，确认后才执行禁用并（若为核心框架）弹出重启强提示。
5. **模组启用与禁用 Toast 提示文本净化精炼**：
   - 移除模组启用/禁用操作成功后浮层提示信息中的 `（原排序保持不变）` 冗余括号说明，提示精简为清晰明了的 `模组【xxx】已启用` 或 `模组【xxx】已禁用`，视觉体验更清爽利落。

---

## v1.0.1 (2026-09-26)

### 优化性更新

1. **模组市场连续安装体验**：从模组市场一键安装模组后选择【稍后重载】，界面不再自动跳离市场页签，玩家可以留在市场中连续浏览并安装多个模组，全部安装完成后手动重新载入即可生效（本地导入模组的「切换管理页并金色高亮」原有设计保持不变）。
2. **历史版本永久保留**：`release/` 归档目录与 GitHub Releases 中的历史版本安装包不再随新版发布而清理，任何旧版本均可追溯与回滚（`../MOD/` 游戏生效目录仍保持单一最新版，防止同名模组多版本并存引发加载冲突）。
3. **管理页模组简介智能补全与动态回填**：针对第三方模组作者未在 `boot.json` 声明中文简介或副标题的情况，全面优化管理页的简介呈现体验。
   - 扩充主流社区模组内置对照词库（涵盖 `ModHub`、`GuideToMe`、`DoLSims`、`Wraith'sReflection`、`NoBusHarassmentMod`、`SimpleFramework`、`Cattery`、`FishingLife` 等数十款模组）；
   - 引入大小写、空格、下划线及单双引号全容错归一化查找机制（`dolOptFindKnownAlias`）；
   - 引入模组市场数据动态回填机制：当本地模组无自带简介且未命中静态词库时，自动联动模组市场统一索引与缓存数据，提取其中文名称或功能简述回填展示；
   - 为 ModHub 自身 `boot.json` 补充中文昵称与描述。
4. **TweeReplacer 补丁冲突快速诊断增强**：在模组加载异常快速诊断中，新增针对 `TweeReplacer` 补丁匹配失败与冲突的精准诊断知识库（`twee-patch-mismatch`）。自动提取报错模组名称与目标段落名称（支持 `[段落] Widgets Clothing Caption` 徽章一键筛选），通俗解析模组间补丁冲突与游戏版本文本差异成因，针对界面入口改写冲突提供专属排查建议。
5. **补丁文件 Unix LF 换行归一化**：全面将 `src/twee/modloader/` 下的所有 `.twee` 补丁文件转换为标准 Unix LF 换行，新增 `.gitattributes` 锁定 LF，并在 `pack.py` 打包时自动确保 `.twee` 规范为 LF 写入，彻底消除因 Windows CRLF 导致后置第三方模组（如原版优化 v1.1.0.6）严格纯文本 LF 匹配断裂的隐患。
6. **顶部操作按钮布局顺序调优与视觉统合**：将【智能整理模组与美化顺序】按钮调整至【刷新列表】左侧，突出高频的智能整理操作；同时为【刷新列表】按钮补齐 `dol-opt-btn-primary` 高亮样式类，实现顶部操作栏全套按钮均具备统一的原生金色边框暗黑高质感。

### 漏洞修复

1. **原版优化图像包占用与依赖解耦修复**：修复与「原版优化」(DolOptimization) 一起安装时原版优化图像无法正常显示的问题（感谢社区贡献者 ANLIN.STUDIO 提交 PR 修复）。
   - 彻底移除 `src/boot.json` 中遗留的 `BeautySelectorAddon` 图像插件配置（`(请确保启用此图像包) 原版优化 DolOptimization`），杜绝无图包的 ModHub 抢占原版优化图像包注册槽位；
   - 移除 `dependenceInfo` 中对 `BeautySelectorAddon` 和 `maplebirch` 的非必要强依赖，仅保留核心 `TweeReplacer` 依赖，实现与原版优化的彻底解耦与零冲突并存。
2. **已安装模组未被模组市场正确识别修复**：修复 `GuideToMe`（对应市场【控制NPC嘴部】）、`DoLSims`（对应市场【模拟人生】）、`Wraith'sReflection`（对应市场【怨灵的倒影】）等模组在本地安装后，模组市场仍显示为未安装、按钮仍显示为【下载安装】的问题。
   - 引入通用的 DoL 生态前缀后缀剥离算法（`stripDoLPrefix`），智能消除仓库名中普遍存在的 `DOL-`、`Degrees-of-Lewdity-`、`scml-` 前缀及 `-mod`、`_mod` 后缀干扰，实现核心标识的自动精准比对；
   - 归一化算法（`normalizeKey`）增强对单双引号（`'`、`’`、`"`、`“`、`”`）与特殊标点的过滤，彻底消除带引号模组（如 `Wraith'sReflection`）的比对偏差；
   - 全面扩充社区知名模组全能别名字典（`DOL_OPT_KNOWN_MOD_MARKET_ALIASES`）与离线仓库映射（`DOL_OPT_OFFLINE_REPOSITORIES`），打通中英文双向映射通道；
   - 将模组匹配查找（`findMarketModByLocalName`）阈值与市场判定严格对齐（80分），确保已安装状态判定零漏报。
3. **已移除 GitHub 仓库与无 Release 模组防误报更新**：修复模组作者删除 GitHub 仓库或未在 GitHub 发布 Release 时，模组市场因 Wiki 历史文本版本误判而错误显示【发现新版】与【一键更新】的问题。
   - 引入通用的仓库有效性检测机制与失效仓库（Dead Repositories）管理体系，支持作者名模糊关联、仓库名解析与 HTTP 404 即时识别，全面覆盖同作者名下全系失效模组（包括 `Kanna-hanabi/WovenRealm`、`Kanna-hanabi/WovenRealmUI` 等关联模组）；
   - 视觉呈现全新引入符合 DoL 暗黑红调色体系的**红色【源已失效】徽章**（`.badge-dead-repo`），卡片清晰标示失效状态，已安装模组操作按钮安静置灰为【已安装】，未安装模组置灰为【暂无下载】；
   - 强化可更新判定（`checkModInstallStatus`），仅在远端具备真实可下载的 Release 产物时才允许触发更新提醒，杜绝纯 Wiki 静态参考版本引发的虚假更新；
   - 运行时遭遇 404 移除时自动友好提示并即时矫正本地状态与界面徽章，彻底杜绝点击更新必然报错且无法自愈的异常死循环。
4. **合法单字符仓库防误诊与缓存自愈清洗**：修复仓库名为单个短横线连字符（如【牧场与你相识的故事】`dawalizhang/-` 与【全新空间】`lingyu230514/-`）被误判为失效源并显示红色标签的问题。
   - 彻底移除对连字符仓库名的错误规则拦截，恢复合法单字符仓库的正常 Release 检索与下载安装；
   - 建立活跃仓库白名单保护，并在市场启动时自动对本地历史存储中的误诊条目执行自愈清洗。
5. **模组市场纯英文缩写偶合误判与 ReadMe 串联修复**：修复未安装 `D.O.L.I` 模组时，模组市场错误将其检测为已安装（且已装版本误读为 `v2.101.0`）、以及本地底层前置模组 `ModLoader DoL ImageLoaderHook` 的 ReadMe 文档页被错误附加上 `D.O.L.I` 市场资料与仓库地址的问题。
   - 根因排查：长词双向包含匹配规则中，纯 ASCII 短串（如 `doli` 长度仅 4）在缺乏单词边界防御时，偶然命中了长技术标识 `modloaderdolimageloaderhook` 中的 `...dol` + `i...` 跨词字母拼接；
   - 匹配防御加固：纯 ASCII（英文/数字）环境严格禁止短于 8 字符的子串跨词匹配，且覆盖率必须达到 80% 以上；中文语义环境下限制最短长度与覆盖率阈值；
   - 彻底杜绝纯英文字符串局部碎片拼合导致的假阳性跨模组串连与版本混淆。

---

## v1.0.0 (2026-09-25)

首个独立发布版本。MOD 管理器模块自「原版优化」(Dol-Optimization) v1.1.0.5 完整解耦，以 ModHub 独立模组形态发布，与原版优化模组彻底分离、互不影响。

### 新功能

1. **模组管理**：旁加载模组的启用、禁用、删除、拖拽排序与智能依赖拓扑排序（含 nickName 别名解析与循环依赖安全回退）。
2. **模组市场**：Wiki 模组列表获取、GitHub Release 检索、国内镜像加速与故障自动切换、本地安装状态智能比对、一键下载旁加载安装。
3. **ReadMe 浏览器**：离线与在线双源 Markdown 渲染阅读，模组内置图片解码与网络图片代理。
4. **加载日志**：启动错误全局捕获、结构化错误诊断、关键词过滤导航与诊断长图一键生成导出。
5. **美化包管理**：承接 BeautySelectorAddon 图像包排序管理与自动启用联动。

### 优化性更新

1. **ModLoader v2.101.1+ 兼容层**：`window.modLoaderGui` 移除后自动映射至 `modModLoadController` + `modUtils` 新 API，旧版调用零感知平滑降级。
2. **持久化安全**：模组列表写入前与存储真实记录合并（已安装未登记模组自动保留防假删除）、写入后回读校验杜绝假成功、`dropNames` 语义保证删除模组不被复活。
3. **孤儿包体自愈**：启动时自动修复「包体已安装、未登记进启用列表」的历史遗留模组。
4. **游戏原生暗黑模态框**：`dolOptConfirm` / `dolOptAlert` 全面替代浏览器原生弹窗，支持危险操作红色语义与键盘操作。
5. **移动端适配**：拖拽排序触控手柄、边缘 RAF 平滑自动滚动、窄屏防挤压布局与底部安全边距。

### 漏洞修复

1. 修复 DataTransfer 构造不受支持环境下批量导入崩溃的问题（自动回退文件数组直传）。
2. 修复市场安装在管理器保存配置期间写入被拒导致的「安装未完成」无提示问题（安装前等待管理器空闲）。
3. 修复安装失败原因不透传的问题（`_dolOptLastInstallError` 诊断信息上报）。

### 解耦变更说明

- 模组标识：`name` 由「原版优化」变更为 `ModHub`，TweeReplacer 补丁 tip 统一改用【ModHub】前缀。
- 文件边界：仅包含模组管理器相关文件（`modloader-optimization.js`、`dol-mod-market.js`、`modloader-optimization.css`、`twee/modloader/*`），不携带原版优化的任何游戏性修改、图像资源与 Twee 补丁。
- 依赖承接：`BeautySelectorAddon` 与 `maplebirch` 依赖声明由 ModHub 承接（原版优化侧已同步移除）。
- 品牌更新：诊断长图水印与模块头部注释统一更新为 ModHub 标识。
